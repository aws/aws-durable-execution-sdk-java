name: AI Issue Implementation

on:
  issue_comment:
    types: [created]
  workflow_dispatch:
    inputs:
      issue-number:
        description: Authorized issue number to implement
        required: true
        type: string

permissions: {}

jobs:
  implement:
    permissions:
      contents: write
      id-token: write
      issues: write
      pull-requests: write
    uses: aws/aws-durable-execution-ci/.github/workflows/ai-issue-implementation.yml@d6b017da14385908951d23e26c790b28a4e5f9f0
    with:
      issue-number: ${{ inputs['issue-number'] || '' }}
    secrets: inherit
